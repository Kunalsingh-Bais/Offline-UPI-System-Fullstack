spring:
  application:
    name: User-service

  datasource:
    url: jdbc:mysql://localhost:3306/upi_user_db
    username: root
    password: Kunalroot123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-register: true
  instance:
    prefer-ip-address: true
